{
  "name": "hogwartsmp-creator-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "HogwartsMP in-game character creator UI (React + TypeScript + Vite). Drives the live player CCC via the framework callEvent bridge (cc:* events).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
